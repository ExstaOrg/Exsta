meta {
  name: GET User
  type: http
  seq: 1
}

get {
  url: {{url}}/api/User
  body: none
  auth: bearer
}

headers {
  {{apim-or-api-key}}: {{user-service-api-key}}
  ~Origin: {{apim-url}}
}

auth:bearer {
  token: {{token}}
}
