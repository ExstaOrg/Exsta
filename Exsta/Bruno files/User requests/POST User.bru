meta {
  name: POST User
  type: http
  seq: 2
}

post {
  url: {{url}}/api/User
  body: json
  auth: bearer
}

headers {
  Content-Type: application/json
}

auth:bearer {
  token: {{token}}
}

body:json {
  {
    "id": 0,
    "username": "string",
    "email": "string",
    "passwordHash": "string",
    "passwordSalt": "string",
    "roles": [
      "user"
    ]
  }
}
